#!/bin/bash -e
# Copyright (c) 2011-2015 TurnKey GNU/Linux - http://www.turnkeylinux.org
# 
# This file is part of buildtasks.
# 
# Buildtasks is free software; you can redistribute it and/or modify it
# under the terms of the GNU Affero General Public License as published by the
# Free Software Foundation; either version 3 of the License, or (at your
# option) any later version.


fatal() { echo "FATAL [$(basename $0)]: $@" 1>&2; exit 1; }
info() { echo "INFO [$(basename $0)]: $@"; }

usage() {
cat<<EOF
Syntax: $0 filepath
Generate GPG signed and checksums signature for filepath (e.g., path/to/iso)

Arguments::

    filepath        - path to file to be signed

Environment::

    BT_CONFIG       - path to buildtasks config directory

EOF
exit 1
}

if [[ "$#" != "1" ]]; then
    usage
fi

filepath=$1

[ -n "$BT_CONFIG" ] || fatal "BT_CONFIG not set"

info "generating gpg signature"
GNUPGHOME=$BT_CONFIG/gnupg gpg -abs --output $filepath.sig.asc $filepath

info "generating md5 checksum"
MD5SUM=$(md5sum $filepath | cut -d " " -f 1)

info "generating sha1 checksum"
SHA1SUM=$(sha1sum $filepath | cut -d " " -f 1)

cat > $filepath.sig <<EOF
This signature file may be used to cryptographically verify file integrity,
like this::

    $ gpg --keyserver hkp://keyserver.ubuntu.com --recv-keys 0xA16EB94D
    $ gpg --list-keys 0xA16EB94D
      pub   2048R/A16EB94D 2008-08-15 [expires: 2023-08-12]
      uid   Turnkey Linux Release Key <release@turnkeylinux.com>

    $ gpg --verify $(basename $filepath).sig
      gpg: Signature made $(date) using RSA key ID A16EB94D
      gpg: Good signature from "Turnkey Linux Release Key"

For your convenience we also include file checksums:

    * sha1sum  $SHA1SUM
    * md5sum   $MD5SUM

You can calculate these on your end and compare to check for errors, but
cryptographic verification is recommended for security reasons.

EOF

info "writing signature file"
cat $filepath.sig.asc >> $filepath.sig
rm $filepath.sig.asc

